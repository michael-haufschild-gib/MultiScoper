1. Purpose

The grid in a pane provides time and amplitude reference lines behind the waveforms.

X-axis: time

Y-axis: amplitude

The grid must:

Redraw correctly when the pane is resized or when the time view changes.

Support two horizontal axis modes:

Absolute time (ms)

Musical time (bars / beats)

Draw horizontal amplitude lines centered around 0.
