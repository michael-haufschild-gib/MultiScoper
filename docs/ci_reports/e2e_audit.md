# E2E Test Suite Coverage Audit

Audit of `tests/e2e/test_*.py` (27 files; the PR audit baseline listed 27 of a claimed 32 — 5 nominal files are not present on disk).

| file | flow tested | assertions | gaps |
|---|---|---|---|
| test_complete_workflows.py | Multi-step full user journeys (new user, multi-osc config, save/load, DAW recall, live perf, bulk ops, theme swap) | osc count; name/mode/visible/opacity/lineWidth post-roundtrip; pane auto-create; waveform peak > threshold; transport still playing after ops; popup-persist matching pre-close values | no real DAW automation/ parameter automation; no undo/redo; no MIDI; no crash/stack recovery; no plugin reinstantiation (DAW reload) |
| test_compound_flows.py | Cross-subsystem interactions (filter+delete, timing persistence, move+save, popup targeting item_1/2, dialog during playback, save during playback, snapshot after ops, color update, rapid interleave) | filtered list updates; paneId in valid set after roundtrip; popup edits target correct osc id; transport still playing; snapshot reflects moves/visibility | no torn-read fuzz under real CPU pressure; no DAW timeline sync; no parameter-group atomicity |
| test_config_popup.py | Popup lifecycle + every popup control (name, source, opacity, lineWidth, visual preset, color picker, pane selector, 6 mode buttons) | popup visible; property propagation with numeric tolerance; ≥4 mode buttons update state; min/max slider boundaries | color-picker click assertion is conditional ("may not change"); no keyboard nav inside popup; no validation error messaging; no undo inside popup; no overflow text in name field |
| test_diagnostic_snapshot.py | /diagnostic/snapshot completeness + consistency + element registry + pane_body/stats_overlay/name label | snapshot has transport/timing/oscillators/sources sections; osc count matches state API; BPM change reflected; core elements registered with non-zero bounds; element deregistration after delete | no schema validation; no version-skew handling; no snapshot under memory pressure; no concurrent snapshot fetch |
| test_edge_cases.py | Dialog+editor close, double-submit, empty state, reorder with 1 item, rapid open/close, state reset, save/load, reorder persistence, delete races, special characters, 10 osc max, close during save/load, API robustness for invalid inputs | no duplicate on double-click; count preserved after rapid ops; special chars roundtrip (XML/ws/music); invalid element IDs return False not crash; 10-osc creation + deletion | upper bound only 10 (no 100/1000 osc); no fuzz on name length > 200; no filesystem full / permission denied; no concurrent multi-client load |
| test_integration_flows.py | Lifecycle + timing/osc interaction + transport/render + pane state + persistence edge cases + color + bulk visibility + mode during playback + editor close with transport active + delete during render | full-state roundtrip; timing ratio 0.5-2 preserved; orderIndex unique after middle delete; color differs between osc | color test has known failure; no test of reopening a saved file from disk across process restart |
| test_keyboard_nav.py | Focus API, tab/shift-tab nav, escape-closes-dialog, rapid keypress stability | focus API returns dict; focused element id matches; focus_next changes focus; escape hides dialog; 20× nav cycle survives | no specific shortcut testing (Cmd+S, Cmd+Z); no screen reader / accessibility labels; no IME/multi-byte keyboard input; no focus trap in modal |
| test_multi_instance.py | Source discovery, cross-instance oscillators, state isolation, multi-editor UI, source lifecycle (add/remove track), cross-instance config, concurrent ops, editor close, state persistence with foreign sources, 3-instance triangles | foreign source id binds; per-track reset isolated; registry deregistration; rebind-to-different-source conditional | no network partition simulation; no version-mismatch plugin coexistence; no multiple DAWs; all on one process |
| test_multi_pane_waveform.py | Per-pane waveform state, cross-pane audio flow, move/pane removal during playback, pane-count sync between APIs | len(panes)>=2; hasWaveformData; peakLevel>0.05; osc count per pane; waveform state pane count == API | pane height / aspect ratio not verified; no verification that audio actually renders pixels in second pane (data-level only) |
| test_options_controls.py | Options-section controls: layout dropdown, grid toggle, autoscale, gain slider, quality preset, buffer duration, auto-adjust, persistence through save/load | dropdowns nonempty; toggle click survives; slider accepts -6..6; value persists through save/reset/load | toggle STATE not verified in most cases ("does not crash"); no buffer resize during capture verification; no audible-artifact check on quality change |
| test_oscillator_crud.py | Add via dialog, cancel behavior, dialog fields, multi-add, delete (list/all/id), edit via popup (name, slider, mode), visibility toggle roundtrip, 6 processing modes | dialog submit creates osc; cancel no-op; delete removes from state+UI; name/lineWidth persist; >=4 modes settable | no undo; no in-place rename via double-click; no batch delete; no context-menu delete |
| test_oscillator_filter.py | All/Visible/Hidden filter tabs + robustness (rapid switch, playback, save/load, visibility toggle, count) | tabs exist; visible filter hides invisible items; hidden filter shows hidden; rapid switch survives; count preserved through save/load | count badges not asserted strictly; no filter search/query; no sort order; no empty-state message when filter results empty |
| test_pane_layout.py | Column layout 1/2/3, column switching, round-robin assignment, dropdown integration, pane move, pane removal redistribution, persistence, playback interaction | pane width = availableArea/N ± 10px; all panes in col 0 in single mode; cycle 1->2->3->1; layout survives save/load and editor lifecycle | no 4+ column layouts; no non-uniform grid (L-shape, spanning); no orientation (rows not tested); no DPI change |
| test_pane_management.py | Auto-create pane, add/remove pane via API, oscillator-to-pane binding, move, invalid pane id, state persistence of pane count + names + bindings | paneId in valid set; custom names survive; move preserves sourceId; invalid move rejected | no pane resize; no pane min/max size constraints; no drag-reorder panes (only via API) |
| test_performance.py | Metrics API (start/stop/reset/double-stop), memory after add-remove cycles, FPS with 5 osc | memory growth < 50MB over 5 cycles; avgFps >= 10 with 5 osc; metrics reset clears counts | only 5-cycle/5-osc load; no sustained (hours) test; no 60 FPS baseline asserted; no GPU memory; no jitter metrics; no frame pacing histogram |
| test_rendering_mode.py | GPU/software toggle: exists, click doesn't crash, with osc active, waveform survives toggle, persistence, rapid cycling | toggle visible; transport still playing; hasWaveformData survives GPU switch; 6× rapid cycle stable | no actual GPU-vs-SW visual comparison; no shader compilation failure path; no driver-loss recovery |
| test_sidebar.py | Accordion expand/collapse, oscillator list selection/expand, delete/settings/vis buttons, sidebar resize, reorder via API | timing content visible after expand & not visible after collapse; buttons have non-zero size; reorder changes order & preserves ids | no persisted accordion state across reopen; no responsive resize below minimum; no drag-reorder via UI covered here (sep. file) |
| test_sixteen_instances.py | 16-instance scale: source discovery, ring topology rendering, sustained playback leak, CRUD stress, editor open/close leak, save/load across all 16, transport global, BPM sweep, idle CPU, sub-linear scaling | 16 unique sources; audio dispatcher >=2 threads + ticks advance; 32 active waveforms no leak; memory < 50/40/60 MB ceilings; CPU(16)/CPU(8)<1.6; per-instance CPU<10% | one process only; no cross-process IPC test; no actual DAW host; no mixed plugin format (VST3+AU+CLAP simultaneously) |
| test_source_list.py | /state/sources API: available, unique IDs, has id/name, oscillator binding | len(sources)>0; ids unique; binding matches; snapshot includes sources | no source metadata (channel count, sample rate); no stale-source cleanup; no permissions/visibility gating |
| test_state_corruption.py | Order-index integrity after add/reorder/delete, snapshot-vs-API consistency, pane integrity under stress, save/load of hidden+float props+multi-pane-moves, BPM persistence | indices unique + non-negative; snapshot name list == state name list; no orphan paneIds; float roundtrip within 0.05/0.1 tolerance | no fuzz generator over operation sequences; no concurrent writer; no mid-save interrupt (SIGTERM); no corrupted-XML recovery path |
| test_status_bar.py | Status-bar labels (fps, cpu, mem, osc count, src count, render mode), updates on osc add/delete, positive FPS during playback, mode label after GPU toggle, survives editor lifecycle | labels registered + visible; count text contains expected digit; ≥3 labels have text during activity | no numerical range validation on CPU/mem (only text presence); no refresh-rate test; no overflow text truncation; no click-to-detail |
| test_theme.py | Theme dropdown populated + select + all selectable + lifecycle persistence + change during playback + rapid cycle + survives save/load | numItems>0; selectedId changes; no failures cycling all; transport continues; selection survives reopen/load | no visual rendering of theme colors verified; no custom/user themes; no color-contrast accessibility checks; no theme hot-reload |
| test_timing.py | Time/melodic toggle, interval field, melodic-mode samples, BPM effect on samples, note dropdown, waveform-mode dropdown, host-sync toggle, timing persists across osc selection + editor reopen | displaySamples>0; 200ms > 100ms samples; 60 BPM > 120 BPM samples; interval ratio 0.5-2 after reopen; toggle state changes | no tempo sync with actual host PPQ; no time-signature; no swing/groove; no bars/beats readout |
| test_transport.py | Transport state, play/stop, position advance/freeze, rapid toggle, BPM boundaries + during playback, position reset, health endpoint, waveform type/freq/amp parametrized | playing flag toggles; positionSamples advances under play, frozen under stop; 20<=BPM<=300; extremes don't crash; waveform params echo | no punch-in/out; no loop regions; no metronome click; no sample-accurate position verification |
| test_ui_interactions.py | Hover, double-click, right-click, scroll, slider increment/decrement/reset, drag-to-reorder, drag_offset on resize handle | element still exists after interaction; double-click doesn't delete; osc count stable; drag changes id order | most are "does not crash" weak assertions; no tooltip content assertion; no context-menu content; no scroll viewport offset verification; no pinch/zoom/gesture |
| test_verification.py | Server-side verify APIs: verify_visible, verify_bounds, verify_color, analyze_waveform, silence waveform data level | sidebar bounds pass; nonexistent returns False; color API returns bool; peak<0.05 for silence (known-failing) | color match not asserted for painted waveform (API contract only); no pixel-diff baseline; no perceptual hash; analyze_waveform dict not schema-checked |
| test_waveform_display.py | Waveform renders with active audio, displaySamples, peak/RMS, update via API, visibility affects render, multi-osc coexistence, silence, freq change, amp -> peak, type switch | peak>0.01; RMS<=peak; displaySamples>100; visible True→False→True; all 3 osc visible; peak drops after amp reduction | amplitude test allows 20s for decay (slow); no phase-correctness test; no FFT/spectrum verification; no sub-sample timing |

## Top gaps across the suite

1. Missing test-harness lifecycle churn — no "load/unload plugin 100×" to catch JUCE component, OpenGL context, or InstanceRegistry leaks across process-stable but plugin-restart scenarios.
2. Missing real DAW host integration — everything runs in the standalone harness; VST3/AU/CLAP wrapper-specific bugs (parameter automation, preset recall, sidechain, bus layouts) are entirely uncovered.
3. Absent visual/pixel regression — `verify_element_color` is tested only as an API contract; no golden-image comparison or perceptual hashing for waveform rendering, theme changes, or 3D shaders.
4. No generative fuzz / property-based coverage on operation sequences — corruption tests use hand-crafted sequences; no fuzzer over (add/delete/move/reorder/save/load) with shrinking.
5. Lacks sustained soak test — longest perf test is 10s; no hours-long run to surface slow leaks, timer drift, or accumulated listener bloat.
6. Does not exercise concurrent HTTP clients — harness is driven serially; a DAW GUI + MIDI + automation threads hitting state simultaneously is not modeled.
7. No failure-injection — missing OpenGL driver-loss, file-system ENOSPC / EACCES on state save, network partition between harness and test, or OOM simulation.
8. Not testing undo/redo — undo stack, edit history, and multi-step undo are not exercised despite being core DAW expectations.
9. Missing accessibility coverage — no screen-reader label assertions, no keyboard-only workflow completion, no color-contrast / WCAG checks, no focus-trap verification in modals.
10. No automated check for parameter automation / MIDI / host-sync — missing host PPQ sync verification, MIDI CC binding, AudioProcessorParameter automation from host, and tempo-map handling.
